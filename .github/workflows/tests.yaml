name: Automated tests

on: pull_request

jobs:
  jest:
    name: Jest Ubuntu
    runs-on: ubuntu-latest
    steps:
      -uses: actions/checkout@v4

      -uses: actions/node@v4
        with:
          node-versio: "lts/hydrogen"
      
      -run: npm ci

      -run: npm test
